<%= form_with(model: observer, local: true) do |form| %>
  <% if observer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(observer.errors.count, "error") %> prohibited this observer from being saved:</h2>

      <ul>
      <% observer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :observer_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
